{
  "name": "carry-over-todos",
  "version": "1.0.0",
  "description": "Obsidian plugin to carry over uncompleted todos from previous daily notes",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "obsidian": "latest",
    "typescript": "^5.0.0",
    "@types/node": "^22.0.0"
  }
}
